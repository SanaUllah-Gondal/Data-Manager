# Data Manager 🗃️

A lightweight, frontend-only web application that allows users to:
- 🔐 Log in with a password (demo mode)
- ➕ Add custom data entries
- ➖ Remove existing entries
- 💾 Data persists locally using `localStorage`

Built with pure **HTML**, **CSS**, and **vanilla JavaScript** — no frameworks or backend required.

> ✅ Ideal for learning CRUD operations and client-side state management.

---

## 🚀 Features
- Simple password-based login (hardcoded for demo)
- Real-time list rendering
- Persistent storage via browser `localStorage`
- Responsive, clean UI

---

## 🛠️ How to Run Locally

1. Clone this repo (once pushed):
   ```bash
   git clone https://github.com/SanaUllah-Gondal/Data-Manager.git
   cd Data-Manager
